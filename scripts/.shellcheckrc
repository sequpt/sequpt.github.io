# ShellCheck v0.11.0 configuration file
# See:
# - Repo: https://github.com/koalaman/shellcheck
# - Configuration file: https://github.com/koalaman/shellcheck/wiki/Directive#shellcheckrc-file
# - Directives: https://github.com/koalaman/shellcheck/wiki/Directive
# - Optional checks: https://github.com/koalaman/shellcheck/wiki/Optional

shell=sh # Set shell type to POSIX sh
external-sources=true # Allow checking files included with `source` and `.`

# Optional checks
enable=add-default-case           # Suggest adding a default case in `case` statements
enable=avoid-nullary-conditions   # Suggest explicitly using `-n` in `[ $var ]`
enable=check-extra-masked-returns # Check for additional cases where exit codes are masked
enable=check-set-e-suppressed     # Notify when `set -e` is suppressed during function invocation
enable=check-unassigned-uppercase # Warn when uppercase variables are unassigned
enable=deprecate-which            # Suggest 'command -v' instead of 'which'
enable=quote-safe-variables       # Suggest quoting variables without metacharacters
enable=useless-use-of-cat         # Check for useless use of cat
